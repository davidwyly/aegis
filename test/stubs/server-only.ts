// Vitest-only stub for Next.js's `server-only` marker. Real `server-only`
// throws on import outside a server component; tests don't care, so this
// no-op lets us import server-side modules from vitest.
export {}
